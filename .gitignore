# IDE / System
.vscode/
.idea/
.agent/
.DS_Store
Thumbs.db
*.log

# Dependencies
node_modules/
.npm/

# Build outputs
dist/
dist_electron/
build/
*.exe
*.dmg
*.AppImage
*.deb
*.zip

# Local data (User history and data bank)
# We want to push the practice_pool data but NOT the user's local attempts if they were saved in files
# Currently everything is in localStorage, so no files to ignore yet.
data set/

# Environment
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Vite
*.tsbuildinfo
